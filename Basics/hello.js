// const math = require('./math')

const math=require("https")//require is used to import other js module/file so that we can use functions of that module in current file.

// console.log(add(9,7));
// console.log(sub(9,7));

console.log(math);
